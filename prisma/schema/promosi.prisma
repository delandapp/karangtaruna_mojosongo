model kalender_konten {
  id               Int       @id @default(autoincrement())
  event_id         Int
  ditugaskan_ke_id Int?
  platform         String
  jenis_konten     String
  caption          String?
  url_media        String?
  hashtag          String?
  dijadwalkan_pada DateTime?
  diterbitkan_pada DateTime?
  reach            Int?
  engagement       Int?
  status           String    @default("ide")
  dibuat_pada      DateTime  @default(now())
  diperbarui_pada  DateTime  @updatedAt
  ditugaskan_ke    m_user?   @relation(fields: [ditugaskan_ke_id], references: [id])
  event            event     @relation(fields: [event_id], references: [id], onDelete: Cascade)

  @@index([event_id])
  @@index([ditugaskan_ke_id])
  @@index([status])
  @@index([platform])
}
